let series = ['sobrenatural', 'breaking bad', 'the soprano'];

// cria se uma variável para salvar o que ela retorna .pop()
var ultimaSerie = series.pop();

console.log(series);
console.log(ultimaSerie);